# Prettier 配置

[prettier](https://prettier.io/) 是一个代码格式化工具，可以自动格式化代码。

当前基于其默认配置覆写了一些配置：

- printWidth 120 每行的字符数限制在120内
- semi false 不添加非必要分号
- singleQuote true 使用单引号
- arrowParens avoid 尽可能省略箭头函数括号
- htmlWhitespaceSensitivity ignore 忽略 HTML 空白敏感性
